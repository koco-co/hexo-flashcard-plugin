{
  "schema_version": "1.0",
  "domain_id": "local-progress",
  "name": "本地进度",
  "status": "confirmed",
  "confirmed_at": "2026-08-24T00:00:00+08:00",
  "dependencies": ["study-review"],
  "summary": "插件用 ts-fsrs repeat/next 完成四档动态调度，只做 Card 序列化适配；进度保存在当前浏览器并按 due 边界恢复。",
  "features": [
    {"id": "F-010", "name": "ts-fsrs 动态调度", "user_inputs": ["当前 FSRS Card、评价和时间点。"], "outputs": {"semantic": ["repeat 预览四档，next 提交所选档；关闭 fuzz 保证预测与提交一致。"]}, "forbidden": ["不得保留固定天数阶梯或自行维护近似公式。"], "acceptance": ["Given 当前卡状态，When 选择记得，Then 保存状态与 Good 预测一致。"]},
    {"id": "F-011", "name": "到期判定与队列状态", "user_inputs": ["FSRS due 与浏览器 now。"], "outputs": {"semantic": ["due <= now 的旧卡进入到期队列，未来旧卡不进入。"]}, "acceptance": ["Given due 等于 now，When 建队列，Then 卡片计入待复习。"]},
    {"id": "F-012", "name": "当前浏览器持久化", "user_inputs": ["稳定卡片 ID 和 ts-fsrs 返回 Card。"], "outputs": {"semantic": ["localStorage v2 保存并恢复原生 FSRS 字段和 lastRating。"]}, "acceptance": ["Given 已保存状态，When 刷新，Then due 与全部调度字段不变。"]},
    {"id": "F-013", "name": "本地边界与重置", "user_inputs": ["读者确认清除本地进度。"], "outputs": {"semantic": ["删除当前浏览器 v2 状态，全部卡恢复新卡；不跨设备同步。"]}, "external_contracts": ["进度作用域：当前浏览器。"], "acceptance": ["Given 当前浏览器有进度，When 确认清除，Then 全部卡恢复新卡且旧 due 消失。"]}
  ],
  "evidence": ["用户于 2026-08-24 明确要求使用成熟 ts-fsrs 并保持插件薄。", "https://github.com/open-spaced-repetition/ts-fsrs/blob/main/packages/fsrs/README.md"]
}
